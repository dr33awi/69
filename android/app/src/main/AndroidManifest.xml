<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <!-- ==================== أذونات الموقع ==================== -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

  <!-- ==================== أذونات الاستشعار ==================== -->
  <uses-permission android:name="android.permission.ACCESS_MAGNETOMETER" />
  
  <!-- ==================== أذونات الإشعارات والتنبيهات ==================== -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.WAKE_LOCK"/>
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
  
  <!-- ==================== أذونات Firebase Cloud Messaging ==================== -->
  <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
  <uses-permission android:name="com.google.android.finsky.permission.BIND_GET_INSTALL_REFERRER_SERVICE" />
  
  <!-- ==================== أذونات الإنترنت والشبكة ==================== -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  
  <!-- ==================== إعلان عدم استخدام المعرّف الإعلاني (Android 13+) ==================== -->
  <!-- يُعلم Google Play بأن التطبيق لا يستخدم المعرّف الإعلاني -->
  <uses-permission android:name="com.google.android.gms.permission.AD_ID" 
      tools:node="remove" />
  
  <!-- ==================== الميزات المطلوبة ==================== -->
  <uses-feature 
      android:name="android.hardware.location" 
      android:required="false" />
  <uses-feature 
      android:name="android.hardware.location.gps" 
      android:required="false" />
  <uses-feature 
      android:name="android.hardware.location.network" 
      android:required="false" />
  
  <uses-feature 
      android:name="android.hardware.sensor.compass" 
      android:required="false" />
  <uses-feature 
      android:name="android.hardware.sensor.accelerometer" 
      android:required="false" />

  <application
      android:label="ذكرني"
      android:name="${applicationName}"
      android:icon="@mipmap/ic_launcher">

      <!-- ==================== خدمات Firebase Cloud Messaging ==================== -->
      
      <service
          android:name="com.dhakarani1.app.MyFirebaseMessagingService"
          android:exported="false"
          android:directBootAware="true">
          <intent-filter>
              <action android:name="com.google.firebase.MESSAGING_EVENT" />
          </intent-filter>
      </service>

      <!-- Firebase Messaging Default Receiver -->
      <receiver
          android:name="com.google.firebase.messaging.FirebaseMessagingReceiver"
          android:exported="true"
          android:permission="com.google.android.c2dm.permission.SEND">
          <intent-filter>
              <action android:name="com.google.android.c2dm.intent.RECEIVE" />
              <category android:name="com.dhakarani1.app" />
          </intent-filter>
      </receiver>

      <!-- ==================== مستقبلات وخدمات الإشعارات المحلية ==================== -->
      
      <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
          android:exported="false">
          <intent-filter>
              <action android:name="android.intent.action.BOOT_COMPLETED"/>
              <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
              <action android:name="android.intent.action.QUICKBOOT_POWERON" />
              <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
          </intent-filter>
      </receiver>
      
      <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" 
          android:exported="false" />

      <!-- ==================== خدمة الموقع في الخلفية ==================== -->
      <service
          android:name="com.dhakarani1.app.LocationBackgroundService"
          android:enabled="true"
          android:exported="false"
          android:foregroundServiceType="location" />

      <!-- ==================== النشاط الرئيسي ==================== -->
      <activity
          android:name=".MainActivity"
          android:exported="true"
          android:launchMode="singleTop"
          android:theme="@style/LaunchTheme"
          android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
          android:hardwareAccelerated="true"
          android:windowSoftInputMode="adjustResize"
          android:showWhenLocked="true"
          android:turnScreenOn="true">
          
          <meta-data
            android:name="io.flutter.embedding.android.DefaultFlutterActivity"
            android:value="true" />
            
          <meta-data
            android:name="io.flutter.embedding.android.SplashScreenDrawable"
            android:resource="@drawable/launch_background"
          />
          
          <meta-data
            android:name="io.flutter.embedding.android.NormalTheme"
            android:resource="@style/NormalTheme"
          />
            
          <intent-filter>
              <action android:name="android.intent.action.MAIN"/>
              <category android:name="android.intent.category.LAUNCHER"/>
          </intent-filter>
          
          <intent-filter>
              <action android:name="FLUTTER_NOTIFICATION_CLICK" />
              <category android:name="android.intent.category.DEFAULT" />
          </intent-filter>

          <intent-filter>
              <action android:name="android.intent.action.VIEW" />
              <category android:name="android.intent.category.DEFAULT" />
              <category android:name="android.intent.category.BROWSABLE" />
              <data android:scheme="athkar" />
          </intent-filter>
          
          <intent-filter>
              <action android:name="android.intent.action.SEND" />
              <category android:name="android.intent.category.DEFAULT" />
              <data android:mimeType="text/plain" />
          </intent-filter>
      </activity>
      
      <!-- ==================== نشاط إعدادات الموقع ==================== -->
      <activity
          android:name=".LocationPermissionActivity"
          android:exported="false"
          android:theme="@style/Theme.AppCompat.Light.Dialog" />
      
      <!-- ==================== إعدادات Firebase Core ==================== -->
      
      <!-- Google Services Version -->
      <meta-data
          android:name="com.google.android.gms.version"
          android:value="@integer/google_play_services_version" />
      
      <!-- ==================== إعدادات Firebase Cloud Messaging ==================== -->
      
      <!-- Firebase Messaging Auto-initialization -->
      <meta-data
          android:name="firebase_messaging_auto_init_enabled"
          android:value="true" />
      
      <!-- Firebase Messaging Default Channel -->
      <meta-data
          android:name="com.google.firebase.messaging.default_notification_channel_id"
          android:value="athkar_channel_id" />

      <!-- Firebase Messaging Default Icon -->
      <meta-data
          android:name="com.google.firebase.messaging.default_notification_icon"
          android:resource="@drawable/ic_notification" />

      <!-- Firebase Messaging Default Color -->
      <meta-data
          android:name="com.google.firebase.messaging.default_notification_color"
          android:resource="@color/notification_color" />

      <!-- ==================== إعدادات Firebase In-App Messaging ==================== -->
      
      <!-- تفعيل In-App Messaging -->
      <meta-data
          android:name="firebase_in_app_messaging_auto_init_enabled"
          android:value="true" />
      
      <!-- تفعيل جمع البيانات التلقائي لـ In-App Messaging -->
      <meta-data
          android:name="firebase_inappmessaging_auto_data_collection_enabled"
          android:value="true" />

      <!-- ==================== إعدادات Firebase Analytics ==================== -->
      
      <!-- تعطيل Analytics في وضع التطوير (اختياري) -->
      <meta-data
          android:name="firebase_analytics_collection_enabled"
          android:value="true" />
      
      <!-- تفعيل جمع البيانات التلقائي -->
      <meta-data
          android:name="firebase_analytics_collection_deactivated"
          android:value="false" />

      <!-- ==================== إعدادات Firebase Crashlytics ==================== -->
      
      <!-- تفعيل Crashlytics -->
      <meta-data
          android:name="firebase_crashlytics_collection_enabled"
          android:value="true" />

      <!-- ==================== إعدادات Firebase Performance ==================== -->
      
      <!-- تفعيل Performance Monitoring -->
      <meta-data
          android:name="firebase_performance_collection_enabled"
          android:value="true" />
      
      <!-- تعطيل Performance في وضع التطوير (اختياري) -->
      <meta-data
          android:name="firebase_performance_collection_deactivated"
          android:value="false" />

      <!-- ==================== إعدادات الموقع ==================== -->
      
      <!-- تفعيل خدمة الموقع في الخلفية -->
      <meta-data
          android:name="location_background_service_enabled"
          android:value="true" />

      <!-- ==================== إعدادات Flutter ==================== -->
      
      <!-- Flutter Embedding Version -->
      <meta-data
          android:name="flutterEmbedding"
          android:value="2" />
      
      <!-- تفعيل تسريع الأجهزة -->
      <meta-data
          android:name="io.flutter.embedding.android.EnableHardwareAcceleration"
          android:value="true" />
      
      <!-- الخط الافتراضي -->
      <meta-data
          android:name="io.flutter.embedding.android.DefaultFontFamily"
          android:value="Cairo" />

  </application>

  <!-- ==================== استعلامات التطبيق (Android 11+) ==================== -->
  <queries>
      <!-- فتح متجر التطبيقات -->
      <intent>
          <action android:name="android.intent.action.VIEW" />
          <data android:scheme="market" />
      </intent>
      
      <!-- فتح روابط HTTPS -->
      <intent>
          <action android:name="android.intent.action.VIEW" />
          <data android:scheme="https" />
      </intent>
      
      <!-- مشاركة النص -->
      <intent>
          <action android:name="android.intent.action.SEND" />
          <data android:mimeType="text/plain" />
      </intent>
      
      <!-- فتح الخرائط -->
      <intent>
          <action android:name="android.intent.action.VIEW" />
          <data android:scheme="geo" />
      </intent>
      
      <!-- فتح إعدادات التطبيق -->
      <intent>
          <action android:name="android.settings.APPLICATION_DETAILS_SETTINGS" />
      </intent>
      
      <!-- فتح إعدادات الموقع -->
      <intent>
          <action android:name="android.settings.LOCATION_SOURCE_SETTINGS" />
      </intent>
  </queries>
  
</manifest>